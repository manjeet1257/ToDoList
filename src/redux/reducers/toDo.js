// src/store/reducers/index.js

// Example reducer
const toDoReducer = (state = {}, action) => {
  switch (action.type) {
    // Define your action cases
    default:
      return state;
  }
};

export { toDoReducer };
